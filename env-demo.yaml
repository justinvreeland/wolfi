package:
  name: env-demo
  version: "0.0.1"
  epoch: 0
  description: Demo package
  copyright:
    - license: APACHE-2.0

environment:
  contents:
    packages:
      - busybox
  environment:
    TEST_0: "TEST_0"

pipeline:
  - uses: env-demo
    environment:
      TEST_1: "TEST_1"

  - runs: env
    environment:
      TEST_2: "TEST_2"

update:
  enabled: true
  release-monitor:
    identifier: 15

